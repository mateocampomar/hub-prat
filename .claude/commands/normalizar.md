Normalizá los clips de `1-fuentes/` que falten en `2-normalizados/`.

Spec: 29.97 CFR (30000/1001), misma resolución y SAR, yuv420p.
Clips con frames duplicados: mpdecimate + minterpolate.

Primero mostrame el comando ffmpeg completo de cada clip. Esperá mi OK antes de ejecutar.
